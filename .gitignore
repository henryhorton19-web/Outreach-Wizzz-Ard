# --- Python ---
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
build/
dist/
*.spec.bak

# --- runtime / logs ---
faults.log
.test_data/
session_stats.json

# --- PRIVATE: personal profile & real outreach voices (never commit) ---
.env
.env.*
engine/config_local.py
app/seed_voices_local/
profile.json
campaign.json
lists.json
queues/

# --- PRIVATE: staged emails (bodies, recipient addresses, embedded attachments) ---
outbox/
*.eml
resume.json

# --- OS noise ---
.DS_Store
Thumbs.db

# --- Stage E: permanent exclusion list (15,592 company names — never commit) ---
excluded.json

# --- Zip archives ---
*.zip


# --- derived market maps / exclusion lists: never commit ---
wizzard_seed_presets_patch/
**/market_map.json
**/data_dir_import/
